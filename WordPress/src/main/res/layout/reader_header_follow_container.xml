<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/text_blog_follow_count"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
        android:textColor="?attr/colorOnSurface"
        app:layout_constraintBottom_toTopOf="@+id/follow_button_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="@dimen/margin_extra_large"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="123 posts • 1.2m followers" />

    <FrameLayout
        android:id="@+id/follow_button_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/text_blog_follow_count">

        <org.wordpress.android.ui.reader.views.ReaderFollowButton
            android:id="@+id/follow_button"
            style="@style/Reader.Follow.Button.New"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center" />

        <ProgressBar
            android:id="@+id/follow_button_progress"
            android:layout_width="@dimen/reader_follow_button_progress_size"
            android:layout_height="@dimen/reader_follow_button_progress_size"
            android:layout_gravity="center"
            android:visibility="gone"
            tools:visibility="visible" />

    </FrameLayout>

    <ImageButton
        android:id="@+id/subscription_settings_button"
        android:layout_width="@dimen/min_touch_target_sz"
        android:layout_height="@dimen/min_touch_target_sz"
        android:layout_marginStart="@dimen/margin_medium"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/reader_subscription_settings_title"
        android:src="@drawable/ic_cog_white_24dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/follow_button_container"
        app:layout_constraintStart_toEndOf="@+id/follow_button_container"
        app:layout_constraintTop_toTopOf="@+id/follow_button_container"
        app:tint="?attr/colorOnSurface"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>

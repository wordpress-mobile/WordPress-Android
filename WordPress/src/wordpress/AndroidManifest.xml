<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="org.wordpress.android.sharedlogin.provider.SharedLoginProvider"
            android:authorities="${applicationId}.SharedLoginProvider"
            android:exported="true" />
    </application>
</manifest>

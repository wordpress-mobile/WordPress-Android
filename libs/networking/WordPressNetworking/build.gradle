buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.1'
    }
}

repositories {
    google()
    jcenter()
    maven { url 'http://wordpress-mobile.github.io/WordPress-Android' }
}

apply plugin: 'com.android.library'
apply plugin: 'maven'

android {
    compileSdkVersion 28
    buildToolsVersion '28.0.3'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 26
        versionName "1.0.0"
    }
}

dependencies {
    implementation 'com.android.volley:volley:1.1.1'

    implementation 'org.wordpress:utils:1.18.1'
    // TODO: Revert to com.automattic:rest
    implementation ('com.github.automattic:android-wordpress-com-rest:2dca7ecd107c8653074d68f28e1b54b4359ee1fe') {
        exclude group: 'com.mcxiaoke.volley'
    }

    lintChecks 'org.wordpress:lint:1.0.1'
}

uploadArchives {
    repositories {
        mavenDeployer {
            def repo_url = ""
            if (project.hasProperty("repository")) {
                repo_url = project.repository
            }
            repository(url: repo_url)
            pom.version = android.defaultConfig.versionName
            pom.groupId = "org.wordpress"
            pom.artifactId = "wordpress-networking"
        }
    }
}

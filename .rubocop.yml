inherit_mode:
  merge:
    - Include

AllCops:
  Exclude:
    - vendor/**/*
  Include:
    - .buildkite/danger/**/*
  NewCops: enable

Metrics/MethodLength:
  Max: 30

Layout/LineLength:
  Max: 180

Naming/FileName:
  Exclude:
    - .buildkite/danger/Dangerfile
